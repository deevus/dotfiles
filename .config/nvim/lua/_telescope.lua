require('telescope').setup{
    defaults = {
        --vimgrep_arguments = {
            --'pt',
            --'--nocolor',
            --'--smart-case',
            --'--nogroup',
            --'--column',
        --}
    },
}
